<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="PartnerDetailsEdit" t-inherit="point_of_sale.PartnerDetailsEdit" t-inherit-mode="extension" owl="1">
		<xpath expr="//div[hasclass('partner-details-left')]" position="inside">
                <link rel="stylesheet" type="text/css" href="/partner_customization/static/src/scss/choices.css"/>

            <div class="partner-detail">
                mobiscroll.select('#multiple-select', {
                    inputElement: document.getElementById('my-input'),
                    touchUi: false
                });
                <span class="label">Tags</span>
                <select class="chosen-select" name="category_id" multiple="multiple"
                        t-on-change="captureChange">
                    <option value="">None</option>
                    <t t-foreach="env.pos.PartnerCategory" t-as="category" t-key="category.id">
                        <option t-att-value="category.id"
                                t-att-selected="props.partner.category_id ? ((country.id === props.partner.category_id[0]) ? true : undefined) : undefined">
                            <t t-esc="category.name" />
                        </option>
                    </t>
                </select>
            </div>
		</xpath>
	</t>
</templates>
	
	
